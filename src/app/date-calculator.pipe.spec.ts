import { DateCalculatorPipe } from './date-calculator.pipe';

describe('DateCalculatorPipe', () => {
  it('create an instance', () => {
    const pipe = new DateCalculatorPipe();
    expect(pipe).toBeTruthy();
  });
});
